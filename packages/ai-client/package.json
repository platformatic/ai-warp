{
  "name": "@platformatic/ai-client",
  "version": "1.1.3",
  "description": "The Platformatic AI client",
  "homepage": "https://github.com/platformatic/ai-warp",
  "author": "Platformatic Inc. <oss@platformatic.dev> (https://platformatic.dev)",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/platformatic/ai-warp.git"
  },
  "keywords": [
    "ai",
    "client"
  ],
  "bugs": {
    "url": "https://github.com/platformatic/ai-warp/issues"
  },
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "ci": "pnpm run lint && pnpm run test:ci && pnpm run typecheck && pnpm run build",
    "test": "c8 -c test/config/c8-local.json node --env-file=test/config/env --no-warnings --test --test-concurrency=1 --test-timeout=300000 'test/**/*.test.ts'",
    "test:ci": "c8 -c test/config/c8-ci.json node --env-file=test/config/env --no-warnings --test --test-concurrency=1 --test-timeout=300000 'test/**/*.test.ts'",
    "lint": "eslint --cache",
    "lint:fix": "eslint --fix",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "check": "pnpm run lint && pnpm run test && pnpm run typecheck && pnpm run build",
    "prepublishOnly": "pnpm run build",
    "postpublish": "git push origin && git push origin -f --tags"
  },
  "devDependencies": {
    "@platformatic/ai-provider": "workspace:*",
    "@types/node": "^22.13.9",
    "c8": "^10.1.3",
    "cleaner-spec-reporter": "^0.5.0",
    "eslint": "^9.21.0",
    "eslint-plugin-unused-imports": "^4.1.4",
    "fastify": "^5.4.0",
    "neostandard": "^0.12.1",
    "pino-pretty": "^13.0.0",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
